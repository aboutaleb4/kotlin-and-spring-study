{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "../gradlew build",
            "options": {
                "cwd": "${workspaceFolder}/basics"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
} 